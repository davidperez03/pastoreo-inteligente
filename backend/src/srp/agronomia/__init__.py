"""Contexto delimitado: Modelado Agronómico (§17.1).

Paquete namespace mínimo a propósito: otras unidades añaden en paralelo
`infra_clima/` e `infra_ndvi/` bajo este mismo paquete. El dominio puro vive
en `srp.agronomia.domain`.
"""
